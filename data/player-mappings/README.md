# Player mappings

Reserved for reviewed external-ID mappings that cannot be derived from the
Sleeper player payload. Runtime code currently creates stable `jfp:<sleeper_id>`
canonical IDs and stores Sleeper, GSIS, ESPN and PFR IDs when supplied.
